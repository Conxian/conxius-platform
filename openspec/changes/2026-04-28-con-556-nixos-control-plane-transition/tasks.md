# Tasks: CON-556 Transition

- [ ] Update `SYSTEM_GRAPH.md` to replace MCC with NixOS Control Plane.
- [ ] Update `README.md` to narrow the repository role to "Declarative Control Plane".
- [ ] Create `nixos/common/default.nix` as a starting point for shared configuration.
- [ ] Add `.sops.yaml` for initial secret management setup.
- [ ] Document the deprecation of `provision-secrets.sh` in `CONTRIBUTING.md`.
