# Spec delta index

The normative change-local delta is
[`specs/fail-closed-verifier-boundaries/spec.md`](specs/fail-closed-verifier-boundaries/spec.md).
It defines the platform boundary only; the future cryptographic backend
contracts remain owned by Gateway/Core/Nexus.
