{
  "name": "conxius-platform",
  "version": "0.2.5",
  "private": true,
  "scripts": {
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm run test:agent-discovery && pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm run typecheck:agent-discovery && pnpm -r --if-present run typecheck",
    "agent-discovery": "tsx scripts/agent-discovery.ts",
    "test:agent-discovery": "tsx --test scripts/agent-discovery.test.ts",
    "typecheck:agent-discovery": "tsc --noEmit --strict --target ES2022 --module ESNext --moduleResolution Bundler --skipLibCheck --ignoreConfig --typeRoots services/elizaos-plugin-conxian/node_modules/@types --types node scripts/agent-discovery.ts scripts/agent-discovery.test.ts",
    "check:dependency-consistency": "node scripts/check-dependency-consistency.mjs",
    "check:lifecycle-control": "bash scripts/ci/run-lifecycle-control-gates.sh",
    "check:phase6:sequence1": "bash scripts/ci/run-cross-repo-harness-mvp.sh",
    "prepublishOnly": "node -e \"console.error('pnpm publish is disabled for now in this repository; use Git tags and GitHub Releases.'); process.exit(1)\"",
    "kb:ingest": "tsx scripts/kb/ingest-github.ts",
    "kb:patterns": "tsx scripts/kb/detect-patterns.ts",
    "kb:research": "tsx scripts/kb/external-research.ts",
    "kb:update": "tsx scripts/kb/generate-updates.ts",
    "kb:status": "tsx -e \"import {knowledgeStore} from './scripts/kb/knowledge-store'; console.log(JSON.stringify(knowledgeStore.getStats(), null, 2))\""
  },
  "packageManager": "pnpm@9.15.5",
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "6.0.3",
    "tsx": "4.23.1",
    "vite": "8.1.5"
  },
  "pnpm": {
    "overrides": {
      "ip-address": ">=10.1.1",
      "postcss": ">=8.5.10",
      "langsmith": ">=0.5.19",
      "uuid": ">=13.0.1",
      "vite": "8.1.5"
    }
  }
}
